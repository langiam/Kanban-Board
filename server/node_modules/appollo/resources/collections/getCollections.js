import axios from "axios";

export const getCollections = async ({ merchantId, limit, page, token }) => {

    try {
        const queryString = `merchantId=${merchantId}${limit ? `&limit=${limit}` : ""}${page ? `&page=${page}` : ""}`

        const response = await axios.get(`https://api.tryappollo.com/collections?${queryString}`,
            {
                headers: {
                    Authorization: `Bearer ${token}`
                }
            })

        return response.data.collections
    } catch (error) {
        return error.response.data
    }

}


