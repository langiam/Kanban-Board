import axios from "axios";

export const deleteCollection = async ({ merchantId, collectionId, token }) => {

    try {
        const response = await axios.delete(`https://api.tryappollo.com/collections/${collectionId}?merchantId=${merchantId}`,
            {
                headers: {
                    Authorization: `Bearer ${token}`
                }
            })

        return response.data.collection
    } catch (error) {
        return error.response.data
    }

}
