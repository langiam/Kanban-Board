import axios from "axios";

export const deleteProduct = async ({ merchantId, productId, token }) => {

    try {
        const response = await axios.delete(`https://api.tryappollo.com/products/${productId}?merchantId=${merchantId}`, {
            headers: {
                Authorization: `Bearer ${token}`
            }
        })

        return response.data.product
    } catch (error) {
        return error.response.data
    }

}
