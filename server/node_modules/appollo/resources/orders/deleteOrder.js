import axios from "axios";

export const deleteOrder = async ({ merchantId, orderId, token }) => {

    try {
        const response = await axios.delete(`https://api.tryappollo.com/orders/${orderId}?merchantId=${merchantId}`,
            {
                headers: {
                    Authorization: `Bearer ${token}`
                }
            })

        return response.data.order
    } catch (error) {
        return error.response.data
    }

}
